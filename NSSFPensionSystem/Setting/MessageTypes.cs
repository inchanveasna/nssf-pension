using System;
namespace NSSFPensionSystem.Setting
{
    public enum MessageTypes
    {
        INFO,
        CONFIRM,
        WARNING,
        ERROR
    }

    public enum MessageText
    {
        REMOVE_CONFIRMATION,
        SUCCESS,
        UNSUCCESS,
        NO_DATA
    }
}
